# 项目根本性指导文件 (META)

> ⚠️ **重要声明**
> 本文件存放经讨论确认的项目概念性、原则性内容。
> 所有内容均经过项目负责人的审查与思考，是项目的根本性指导。
> **未经项目负责人明确授权，不得编辑本文件。**

---

## 实验设计原则

### 1. 核心概念

#### 1.1 MIM与插补的关系

**关键认知**：MIM（缺失指示器方法）与插补方法**不是互斥的对比关系**，而是**正交的组合维度**。

**事实**：
1. **模型必须有输入**：神经网络无法处理"无输入"，缺失位置必须填入某个值
2. **MIM不解决输入值问题**：MIM只是增加了16维的缺失指示器（mask），告诉模型"这个位置原本缺失"
3. **缺失值仍需填充**：特征向量的缺失位置仍需要填入具体数值（0、均值、KNN估计等）

**因此**：
- MIM方法实际上是 **"插补 + MIM指示器"** 的组合
- 对比研究的是：**使用MIM指示器** vs **不使用MIM指示器**
- 两者都需要配合某种插补策略

#### 1.2 实验对比的本质

研究问题是：在**相同插补策略**下，**添加MIM指示器**是否有帮助？

```
对比维度：使用MIM指示器（是/否）
         ↓
实验组：[插补值 + MIM指示器] → 32维输入
对照组：[插补值]             → 16维输入
         ↓
控制变量：两者使用相同的插补策略
```

#### 1.3 完整的2×4实验矩阵

理论上，所有插补方法都可以与MIM结合：

| use_mim ↓ / Imputation → | **mean** | **knn** | **iterative** | **zero** |
|--------------------------|----------|---------|---------------|----------|
| **false**（无MIM） | 16维 | 16维 | 16维 | 16维 |
| **true**（有MIM） | 32维 | 32维 | 32维 | 32维 |

**研究问题**：
1. 主效应：使用MIM指示器是否提升性能？（use_mim: true vs false）
2. 插补效应：不同插补策略的性能差异？
3. 交互效应：MIM增益是否依赖于插补策略？（2×4方差分析）

---

### 1.2 插补方法 (mean, knn, iterative, zero)

**核心原则**：插补是**预测阶段的数据预处理手段**，不影响训练阶段。

| 阶段 | 数据处理方式 | 说明 |
|------|------------|------|
| **训练** | 使用完整数据（无任何缺失） | 模型学习的是完整数据的特征-标签映射 |
| **验证** | 使用完整数据（无任何缺失） | 早停/调参基于完整数据性能 |
| **测试** | 按测试MR生成缺失 → 插补 → 预测 | 插补仅在测试时应用 |

**原则**：插补方法在训练时不应引入缺失，一次训练即可测试多种缺失率。

---

### 1.3 MIM方法（插补 + 缺失指示器）

**核心原则**：MIM方法在**训练时始终使用MCAR生成多MR数据**（无法预知真实缺失模式），在**测试时面对各种缺失模式**。

| 阶段 | 数据处理方式 | 缺失率范围 | 缺失模式 | 说明 |
|------|------------|-----------|---------|------|
| **训练** | MCAR生成多MR + 插补 + 拼接掩码 | 0.0-0.95 | **固定MCAR** | 训练时无法预知真实缺失模式 |
| **验证** | MCAR生成多MR + 插补 + 拼接掩码 | 0.0-0.95 | **固定MCAR** | 多MR平均性能用于早停 |
| **测试** | 各种Mode生成缺失 + 插补 + 拼接掩码 | 0.0-0.95 | MCAR/MAR/MNAR | 测试泛化能力 |

**组成**：
- **插补部分**：填充缺失位置（zero/mean/knn/iterative）
- **指示器部分**：16维缺失掩码（mask）
- **总输入**：32维（16插补后特征 + 16掩码）

**关键理解**：
- MIM方法**仍然进行插补**
- 与纯插补的区别：**额外输入缺失掩码**
- 对比维度：**有指示器** vs **无指示器**

---

## 2. 实验逻辑循环架构

### 2.1 层次结构（从高到低）与分界线

**分界线原则**：
- **分界线以上**：影响神经网络模型训练（模型结构、输入维度、训练数据、验证策略）
- **分界线以下**：不影响模型训练，仅在测试阶段应用

```
╔══════════════════════════════════════════════════════════════╗
║  分界线以上：影响模型训练                                     ║
╠══════════════════════════════════════════════════════════════╣
Level 1: 随机种子 (Random Seed)
    └── 控制整个实验的可复现性
    └── 作为实验的"根"参数，确保所有随机性一致
    └── 影响: L3数据分割、L4模型初始化、L6缺失生成、训练过程
    └── 注意: L2 Dataset是固定的，seed从L3开始实际参与，但放在最外层作为全局控制

Level 2: 数据集 (Dataset)
    └── XJTU数据集（固定，所有实验使用）

Level 3: 电池批次 (Battery Batch)
    └── 6个批次: 2C, 3C, R2.5, R3, RW, Sim_satellite
    └── 每个批次是独立分布，必须分别训练模型

Level 4: 模型架构 (Model Architecture)
    └── 3种架构: MLP, LSTM, CNN
    └── 决定网络结构（层数、隐藏单元等）

Level 5: MIM使用 (use_mim)
    └── 2种设置: false（无MIM）, true（有MIM）
    └── ⚠️ 关键影响：
        ├── 输入维度：16维 vs 32维
        ├── 训练数据：完整数据 vs MCAR多MR混合
        └── 验证策略：完整验证 vs 多MR平均验证

Level 6: 训练缺失率 (Training MR)
    └── use_mim=false：固定为0.0（完整数据训练）
    └── use_mim=true：0.0-0.95（多MR混合训练，步长0.05）
    └── 影响训练数据分布，必须独立训练
╠══════════════════════════════════════════════════════════════╣
║  分界线：训练完成，模型固定                                   ║
╠══════════════════════════════════════════════════════════════╣
Level 7: 缺失模式 (Missingness Mode)
    └── 3种模式: MCAR, MAR, MNAR
    └── 仅在测试阶段应用：决定缺失生成的机制
    └── 与测试MR共同决定缺失数据分布

Level 8: 测试缺失率 (Testing MR)
    └── 20个级别: 0.0, 0.05, ..., 0.95
    └── 仅在测试阶段应用：缺失的程度
    └── 与缺失模式共同决定缺失数据分布
    └── 与训练MR独立，可评估泛化能力

Level 9: 插补方法 (Imputation Method)
    └── 4种方法: mean, knn, iterative, zero
    └── 仅在测试阶段应用：如何填充缺失值
    └── 处理由Mode和测试MR共同生成的缺失数据
    └── 不影响已训练模型的参数
╚══════════════════════════════════════════════════════════════╝
```

**分界线的重要性**：
- 分界线以上（L1-L6）：每个组合需要**独立训练**一个模型
- 分界线以下（L7-L9）：同一模型可以**复用**测试所有组合
- 训练实验数 = 分界线以上的组合数
- 测试结果数 = 分界线以上 × 分界线以下的组合数

**关键洞察**：
- **训练缺失率**（L6）与**测试缺失率**（L8）是独立的
- use_mim=false：训练MR=0.0，测试MR=0.0-0.95
- use_mim=true：训练MR=0.0-0.95（多MR，步长0.05），测试MR=0.0-0.95
- 这种分离允许模型在一种缺失率分布上训练，在另一种缺失率上测试

---

### 2.2 实验执行逻辑

```python
# ═══════════════════════════════════════════════════════════════
# 分界线以上：训练阶段（L1-L6）
# 每个组合训练独立的模型
# ═══════════════════════════════════════════════════════════════
for seed in [42]:                                         # Level 1
    for batch in [2C, 3C, R2.5, R3, RW, Sim_satellite]:  # Level 3
        for model in [mlp, lstm, cnn]:                    # Level 4
            for use_mim in [False, True]:                 # Level 5
                
                # Level 6: 训练缺失率（由use_mim决定）
                if use_mim == False:
                    # 训练MR = 0.0（完整数据）
                    model = train(
                        input_dim=16,
                        train_data='full',  # MR=0.0
                        val_data='full'
                    )
                else:
                    # 训练MR = 0.0-0.95（多MR混合，步长0.05）
                    model = train(
                        input_dim=32,
                        train_data='multi_mr_mcar',  # 多MR混合
                        val_data='multi_mr_mcar'
                    )
                
                save_model(seed, batch, model, use_mim)

# ═══════════════════════════════════════════════════════════════
# 分界线：训练完成，模型参数固定
# ═══════════════════════════════════════════════════════════════

# ═══════════════════════════════════════════════════════════════
# 分界线以下：测试阶段（L7-L9）
# 同一模型复用测试所有组合
# ═══════════════════════════════════════════════════════════════
for seed in [42]:                                         # Level 1
    for batch in [2C, 3C, R2.5, R3, RW, Sim_satellite]:  # Level 3
        for model in [mlp, lstm, cnn]:                    # Level 4
            for use_mim in [False, True]:                 # Level 5
                
                # 加载已训练的模型（分界线以上已固定）
                model = load_model(seed, batch, model, use_mim)
                
                # 分界线以下：遍历所有测试条件
                # L7 + L8 共同决定缺失数据生成
                for mode in [mcar, mar, mnar]:            # Level 7: 缺失机制
                    for test_mr in [0.0, 0.05, ..., 0.95]:  # Level 8: 缺失程度
                        
                        # 生成测试数据（由mode和test_mr共同决定）
                        test_data = create_missing(mode, test_mr)
                        
                        # L9: 对生成的缺失数据应用不同插补方法
                        for imputation in [mean, knn, iterative, zero]:  # Level 9
                            test_data_imp = impute(test_data, imputation)
                            
                            if use_mim == False:
                                # 无MIM：直接评估（16维）
                                result = evaluate(model, test_data_imp)
                            else:
                                # 有MIM：拼接掩码后评估（32维）
                                mask = get_mask(test_data)
                                test_data_mim = concat([test_data_imp, mask])
                                result = evaluate(model, test_data_mim)
```

---

### 2.3 实验数量计算

**分界线以上（L1-L6，影响训练）**：

| 层次 | 变量 | 单种子计算 | 说明 |
|------|------|-----------|------|
| L1: Seed | 1个 | ×1 | 根参数，控制所有随机性 |
| L3: Batch | 6个 | ×6 | 数据分布不同 |
| L4: Model | 3种 | ×3 | 架构不同 |
| L5: use_mim | 2种 | ×2 | 核心影响 |
| L6: 训练MR | - | - | 由use_mim隐含：false=0.0，true=0.0-0.95（步长0.05） |
| **训练实验数** | - | **36** | 每种配置独立训练 |

**分界线以下（L7-L9，仅测试）**：

| 层次 | 变量 | 每模型测试组合 | 说明 |
|------|------|---------------|------|
| L7: Mode | 3种 | ×3 | 缺失生成机制 |
| L8: 测试MR | 20个 | ×20 | 缺失程度，与训练MR独立 |
| L9: Imputation | 4种 | ×4 | 插补处理方法 |
| **每模型测试数** | - | **120** | 可任意组合 |

**总测试结果**：36 模型 × 120 测试 = **4,320 行**

**层级逻辑**：
- **L7 + L8**：Mode 和测试MR 共同决定缺失数据如何生成
- **L9**：对生成的缺失数据应用不同插补方法处理

**训练MR与测试MR的独立性**：
- use_mim=false：训练MR=0.0，测试MR=0.0-0.95（20种）
- use_mim=true：训练MR=0.0-0.95（多MR），测试MR=0.0-0.95（20种）
- 这种分离允许评估模型对训练时未见过的缺失率的泛化能力

**多种子扩展**（如3个种子）：
- 训练实验：36 × 3 = **108**
- 测试结果行：4,320 × 3 = **12,960**

---

### 2.4 关键设计决策

**1. 为何Seed在最外层？**

**核心原因**：Seed是实验的"根"参数，控制整个实验的随机性。

虽然L2 Dataset是固定的（不需要seed），但seed从L3开始实际参与：
- **L3 Batch**：数据分割需要seed
- **L4 Model**：权重初始化需要seed  
- **L6 训练MR**（use_mim=true时）：缺失生成需要seed
- **训练过程**：dropout、batch shuffle等需要seed

**设计理由**：
- **可复现性单位**：一个seed对应一次完整的"实验运行"
- **全局一致性**：确保从数据分割到模型训练的所有随机性使用同一seed
- **统计意义**：多种子（[42, 123, 456]）时，每个seed独立运行完整矩阵，用于计算性能指标的均值和方差
- **避免混淆**：防止不同seed的配置交叉污染

**为什么不放在L2之后？**
虽然seed从L3开始实际参与，但作为全局控制参数，放在最外层更符合实验设计惯例，强调seed是"实验运行的标识"而非"实验变量"。

**2. 为何Batch在Dataset之下？**
- 每个批次是独立的数据分布（不同充放电条件）
- 批次间无数据共享，必须独立训练
- 但在同一seed下，各批次的数据分割保持一致性

**3. 为何Mode在Model之下？**
- **本质**：Mode是测试阶段的缺失生成策略（MCAR/MAR/MNAR）
- 类似于Imputation，是"如何处理缺失"的策略选择
- **所有方法训练都与Mode无关**：
  - 插补方法：用完整数据训练
  - MIM方法：固定用MCAR生成多MR训练（无法预知真实缺失模式）
- **Mode仅在测试阶段应用**：评估模型在各种缺失模式下的泛化能力
- 所有模型都跨Mode复用

**4. 为何测试MR在Imputation之前？**
- **逻辑顺序**：Mode + 测试MR → 共同生成缺失数据 → Imputation处理缺失
- Mode和测试MR是**并列的缺失生成参数**
- Imputation**依赖于**两者的结果

**5. 层次结构对比总结**

| 方法类型 | 分界线以上 | 分界线以下 |
|---------|-----------|-----------|
| **插补方法** | Seed, Batch, Model, use_mim=false | Mode, 测试MR, Imputation |
| **MIM方法** | Seed, Batch, Model, use_mim=true | Mode, 测试MR, Imputation |

**关键**：两种方法的主要区别在于use_mim设置（分界线以上），而测试流程相同（分界线以下）。

---

## 3. 设计合理性说明

### 3.1 为什么use_mim=false时训练用完整数据？

1. **公平性**：插补方法的目的是在预测时修复缺失，训练时应让模型学习最优的完整数据映射
2. **一致性**：如果训练时也按MR插补，相当于训练集和测试集分布不同（数据泄露问题）
3. **简洁性**：一次训练即可测试多种MR和多种插补策略

### 3.2 为什么use_mim=true时训练固定MCAR？

1. **不可预知性**：训练时无法知道真实世界的缺失模式（MCAR/MAR/MNAR）
2. **公平基础**：MCAR是最基础的缺失模式，用MCAR训练相当于"不假设任何特定模式"
3. **泛化测试**：固定MCAR训练后，在MCAR/MAR/MNAR上测试，评估模型对各种模式的适应能力
4. **机制激活**：MIM的掩码通道需要缺失来激活，多MR训练确保各种缺失程度都被学习

### 3.3 为什么use_mim=false时不需要多MR训练？

1. **插补恢复完整性**：插补后数据恢复为"完整"形式，模型学习的是完整数据映射
2. **无需适应缺失**：插补方法的责任是"修复"缺失，模型只负责学习修复后的数据
3. **验证一致性**：验证集也用完整数据，与训练分布一致

### 3.4 为什么use_mim=true时仍需要插补？

1. **模型必须有输入**：神经网络无法处理"无输入"，缺失位置必须填入值
2. **MIM仅提供掩码**：指示器告诉模型"哪里缺失"，但不解决"填什么"
3. **输入维度要求**：即使使用MIM，原始特征维度仍需填满（16维）
4. **组合策略**：插补（填值）+ MIM（告知缺失位置）= 完整解决方案

### 3.5 为什么所有插补方法都在use_mim=true/false下测试？

1. **系统评估**：了解MIM增益是否依赖于插补策略
2. **寻找最佳组合**：确定哪种（插补策略，MIM使用）组合最优
3. **交互效应分析**：2×4方差分析揭示主效应和交互效应

---

## 4. 研究问题定义

### 4.1 主效应（Main Effect）

**问题**：使用MIM指示器是否能提升SOH预测性能？

**对比**：use_mim=true vs use_mim=false（平均所有插补方法）

**研究假设**：MIM指示器提供额外信息，预期能提升性能

### 4.2 插补策略效应（Imputation Effect）

**问题**：不同插补策略的性能差异？

**对比**：mean vs knn vs iterative vs zero（分别在有/无MIM下）

**研究假设**：复杂插补（knn/iterative）可能优于简单插补（mean/zero）

### 4.3 交互效应（Interaction Effect）

**问题**：MIM的增益是否依赖于插补策略？

**对比**：MIM+mean vs MIM+zero vs ...

**分析方法**：2×4方差分析

**研究假设**：MIM可能对某些插补策略增益更大

---

## 文件修改历史

| 日期 | 修改内容 | 授权人 |
|------|---------|--------|
| 2026-03-18 | 初始创建，记录实验设计原则讨论 | chen |
| 2026-03-18 | 添加插补方法与模型的解耦原则 | chen |
| 2026-03-18 | 明确MIM方法三阶段都需要0.0-0.95的20个缺失率 | chen |
| 2026-03-18 | 添加实验逻辑循环架构（层次结构与循环关系） | chen |
| 2026-03-18 | 随机种子迁移到最外层 | chen |
| 2026-03-18 | 缺失模式迁移到模型架构之下 | chen |
| 2026-03-18 | 明确MIM训练固定MCAR，测试面对各种缺失模式 | chen |
| 2026-03-18 | **重要概念澄清**：MIM与插补不是对立的，而是正交维度 | chen |
| 2026-03-18 | **实验架构重构**：方法层→use_mim层+插补层，完整2×4矩阵 | chen |
| 2026-03-18 | **引入分界线原则**：use_mim提至L5，明确区分训练/测试层 | chen |
| 2026-03-18 | **缺失率分层**：拆分为训练MR（L6）和测试MR（L8） | chen |
| 2026-03-18 | **层级逻辑优化**：测试MR移至L8，与Mode共同构成缺失生成层 | chen |
| 2026-03-18 | **清理文档**：删除当前实现描述，仅保留面向未来的设计原则 | chen |

---

*本文件为项目根本性指导，任何代码实现必须符合上述原则。*
