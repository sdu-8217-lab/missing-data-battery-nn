# @package _global_
defaults:
  - _self_
  - data: xjtu
  - model: mlp
  - missing: mcar

experiment:
  name: "battery_soh_experiment"
  seeds: [42, 43, 44, 45, 46]
  output_dir: "./results"

training:
  epochs: 100
  batch_size: 64
  learning_rate: 0.001
  weight_decay: 0.0
  patience: 15
  device: "auto"  # auto, cuda, cpu

wandb:
  enabled: true
  project: "battery-soh"
  entity: null
  tags: []

logging:
  level: "INFO"
