# @package model
# Paper 精确配置 - GRU
name: gru
type: gru

# Paper 架构: 2-layer GRU → FC
# Baseline: 40,769 params, MIM: 43,841 params
input_dim: 16  # 会被 main.py 覆盖为 16 或 32
hidden_size: 64
num_layers: 2
dropout: 0.2
batch_first: true

# 序列设置
seq_len: 5

# Paper 目标参数量（仅供参考）
expected_params_baseline: 40769
expected_params_mim: 49985  # 论文目标
expected_params_mim_actual: 43841  # PyTorch实际计算
