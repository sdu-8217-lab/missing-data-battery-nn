# @package model
# Paper 精确配置 - CNN1D
name: cnn1d
type: cnn1d

# Paper 架构: Conv1d(16→72→32) → FC, kernel=4
# Baseline: 13,961 params (实际), MIM: 18,569 params (实际)
# Paper目标: Baseline=16,713, MIM=30,537
input_dim: 16  # 会被 main.py 覆盖为 16 或 32
channels: [72, 32]
kernel_size: 4
dropout: 0.1

# Paper 目标参数量（仅供参考）
expected_params_baseline: 16713  # 论文目标
expected_params_baseline_actual: 13961  # PyTorch实际计算
expected_params_mim: 30537  # 论文目标
expected_params_mim_actual: 18569  # PyTorch实际计算
