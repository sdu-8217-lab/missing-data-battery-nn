# @package model
# Paper 精确配置 - LSTM
name: lstm
type: lstm

# Paper 架构: 2-layer LSTM → FC
# Baseline: 31,537 params, MIM: 34,609 params
input_dim: 16  # 会被 main.py 覆盖为 16 或 32
hidden_size: 48
num_layers: 2
dropout: 0.2
batch_first: true

# 序列设置
seq_len: 5

# Paper 目标参数量（仅供参考）
expected_params_baseline: 31537
expected_params_mim: 40753  # 论文目标
expected_params_mim_actual: 34609  # PyTorch实际计算
