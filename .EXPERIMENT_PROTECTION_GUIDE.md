# 实验保护机制说明

## 概述

当前项目正在运行**100种子完整实验**（PID: 409674），预计耗时10-15小时。

为防止误操作导致实验失败，已启用**实验保护机制**。

## 保护范围

### 受保护文件（禁止修改）
- `experiments/run_batch_experiments.py` - 批量实验主程序
- `experiments/run_experiment.py` - 单次实验程序
- `experiments/run_single.py` - 单次运行工具
- `experiments/train.py` - 训练脚本
- `src/` - 所有源代码
- `models/100seeds/` - 实验生成的模型文件
- `results/100seeds/` - 实验结果文件
- `logs/experiments/100seeds_*.log` - 实验日志

### 允许修改的文件
- `docs/` - 所有文档（包括重构工作）
- `configs/` - 配置文件（当前实验不使用）
- `README.md` - 项目说明
- `meta.md` - 架构定义
- `.gitignore` - Git配置
- 其他非代码文件

## 保护机制工作原理

### 1. 标记文件 (`.EXPERIMENT_IN_PROGRESS`)
- 位置：项目根目录
- 作用：标识实验正在进行
- 删除时机：实验完成后手动删除

### 2. Git预提交钩子 (`.git/hooks/pre-commit`)
- 位置：`.git/hooks/pre-commit`
- 作用：每次提交前自动检查
- 行为：
  - 如果修改了受保护文件 → 阻止提交
  - 如果只修改了文档/配置 → 允许提交

## 常见问题

### Q1: 如何查看实验进度？
```bash
# 查看实时日志
tail -f logs/experiments/100seeds_*.log

# 查看进程状态
ps aux | grep run_batch_experiments

# 查看统计
grep "Training:" logs/experiments/100seeds_*.log | tail -5
```

### Q2: 我需要修改受保护文件怎么办？

**选项A：等待实验完成（推荐）**
```bash
# 监控实验进度
tail -f logs/experiments/100seeds_*.log
# 当看到 "Experiment Run Complete" 时即可
```

**选项B：紧急修复（风险自担）**
```bash
# 使用 --no-verify 跳过检查
# ⚠️  警告：确保你知道自己在做什么！
git commit -m "紧急修复xxx" --no-verify
```

### Q3: 实验似乎卡住了，如何确认？
```bash
# 检查进程是否存在
ps -p 409674

# 如果进程不存在，可能是实验已完成或崩溃
# 检查日志最后几行
tail -n 20 logs/experiments/100seeds_*.log
```

### Q4: 如何禁用保护机制？

**实验完成后执行：**
```bash
# 1. 删除标记文件
rm .EXPERIMENT_IN_PROGRESS

# 2. （可选）删除预提交钩子
rm .git/hooks/pre-commit
# 或保留钩子，它会自动检测到标记文件不存在

# 3. 删除本说明（可选）
rm .EXPERIMENT_PROTECTION_GUIDE.md
```

## 技术细节

### 受保护路径匹配规则
```regex
^experiments/run_batch_experiments\.py
^experiments/run_experiment\.py
^experiments/run_single\.py
^experiments/train\.py
^src/
^models/100seeds/
^results/100seeds/
^logs/experiments/100seeds
```

### 钩子脚本位置
```
.git/hooks/pre-commit
```

### 钩子脚本功能
1. 检查 `.EXPERIMENT_IN_PROGRESS` 是否存在
2. 检查实验进程是否仍在运行
3. 获取暂存区文件列表
4. 匹配受保护路径模式
5. 如发现受保护文件被修改，阻止提交并提示

## 联系信息

如有紧急情况，请联系：项目负责人
