# 项目文档审查报告

**审查日期**: 2026-02-26
**审查者**: 实验爪 (Lab-Claw)
**审查视角**: 科研工作者批判性审查

---

## 一、严重过时的文档（需立即更新）

### 1. AGENTS.md ❌ 严重过时

**问题**: 该文档描述的是旧架构，与当前代码完全不匹配

**具体错误**:
| 提及的文件/概念 | 实际情况 | 严重程度 |
|----------------|---------|---------|
| `src/config/experiment_config.py` | ❌ 已删除 | 🔴 高 |
| `run_single.py`, `run_batch.py` | ❌ 不存在 | 🔴 高 |
| `Baseline: 仅使用完整数据训练` | ❌ 定义已改 | 🔴 高 |
| `src/data/dataset_loader.py` | ✅ 存在但非主入口 | 🟡 中 |
| `src/models/model_factory.py` | ❌ 不存在 | 🔴 高 |
| `XGBoost` | ❌ 已从模型列表移除 | 🟡 中 |
| 项目结构描述 | ❌ 大量文件已不存在 | 🔴 高 |

**结论**: 该文档误导性极强，建议重写或删除

---

### 2. ARCHITECTURE.md ❌ 多处错误

**问题**: MAR 公式描述错误 + 文件结构过时

**具体错误**:
1. **MAR 公式错误** (第 56 行):
   - 文档: `p_i = alpha * (1 - SOH_i)^beta + gamma`
   - 实际: 已修正为 `p_i = alpha * SOH_i^beta + gamma`
   - 影响: 公式方向完全相反，会误导读者

2. **文件结构描述** (第 65-90 行):
   - 提到 `src/models/base_model.py` ❌ 不存在
   - 提到 `src/models/model_factory.py` ❌ 不存在
   - 提到 `src/trainers/lightning_trainer.py` ❌ 不存在

3. **使用方式** (第 128-142 行):
   - 提到 `experiment=mim_mar_0.3` 配置
   - 实际: 已改为 `method=mim` 参数方式

**结论**: 需要全面更新

---

### 3. docs/01-分层实验架构设计.md ❌ 架构已废弃

**问题**: 描述的 "SingleExperiment/BatchExperiment" 分层架构已废弃

**实际情况**:
- 现在使用 Hydra + Lightning 的统一入口 `src/main.py`
- 不再有小实验/大实验的分层概念
- 实验通过 `method=mim/mean/knn` 参数控制

**结论**: 该文档已完全过时，建议标记为 "历史文档"

---

## 二、需要修正的文档

### 4. README.md 🟡 部分过时

**问题**:
1. **使用方式** (第 25 行):
   ```bash
   python src/main.py data=xjtu model=mlp missing=mcar model.use_mim=true
   ```
   - 问题: `model.use_mim=true` 已改为 `method=mim`
   - 建议改为: `python src/main.py data=xjtu model=mlp method=mim`

2. **实验配置示例** (第 87-100 行):
   - 提到 `experiment=mim_mar_0.3.yaml`
   - 实际: 不再使用单独的实验配置文件
   - 建议移除或改为当前配置方式说明

3. **缺失机制描述** (第 55-60 行):
   - 缺少对传统插补 baseline 的说明
   - 建议添加: "对比方法包括 MIM 和 Mean/Median/KNN/Zero 插补"

**结论**: 需要小幅更新

---

### 5. docs/MIM_CORRECTION_SUMMARY.md 🟡 路径过时

**问题**: 
- 第 15 行: `src/trainers/neural_network_trainer.py` ❌ 不存在
- 实际: MIM 训练逻辑现在在 `src/data/loader_dataloaders.py`

**结论**: 路径引用需要更新

---

## 三、内容存疑的文档

### 6. docs/09-论文大纲.md ❓ 需要确认

**存疑内容**:
1. **模型组合** (第 25-78 行):
   - 推荐包含 XGBoost
   - **疑问**: 是否还需要 XGBoost？当前代码已移除，如需对比需重新添加

2. **序列长度** (第 57 行):
   - 提到 "用最近 3-5 个循环"
   - **疑问**: 当前固定为 5，是否还需要测试 3？

3. **实验设计** (全文):
   - 基于旧架构描述
   - **疑问**: 是否需要重写以匹配新架构？

**需要用户确认**:
- [ ] 是否需要 XGBoost baseline？
- [ ] 是否需要测试 seq_len=3？
- [ ] 是否保留此大纲，还是重写？

---

### 7. docs/11-文档一致性检查.md ❓ 状态不明

**存疑内容**:
1. **模型参数表** (第 10-22 行):
   - 描述的模型配置与实际代码中的配置是否一致？
   - 如: LSTM hidden=42 vs 当前代码中的 48

2. **建议补充的文档** (第 40-120 行):
   - 这些建议是否仍然有效？
   - 部分建议 (如 "数据预处理规范") 可能已过时

**需要验证**:
- [ ] 模型参数表与实际代码一致性
- [ ] 补充文档建议的优先级

---

## 四、建议处理方案

### 立即处理 (本周)
1. **重写 AGENTS.md** - 完全过时，误导性强
2. **修正 README.md** - 主要入口，使用频率高
3. **修正 ARCHITECTURE.md** - MAR 公式错误严重

### 后续处理 (下周)
4. **标记旧文档** - 01-分层实验架构设计.md 等标记为历史文档
5. **更新路径引用** - MIM_CORRECTION_SUMMARY.md 等
6. **确认论文大纲** - 与用户讨论 09-论文大纲.md 的适用性

### 长期维护
7. **建立文档检查机制** - 每次代码变更时同步更新文档
8. **文档版本控制** - 在文档中明确标注最后更新日期和代码版本

---

## 五、关键矛盾点总结

| 矛盾点 | 位置 | 影响 |
|--------|------|------|
| Baseline 定义改变 | AGENTS.md, 多处 | 读者可能误解什么是 baseline |
| MAR 公式方向 | ARCHITECTURE.md | 公式物理意义完全相反 |
| 使用方法 | README.md | 命令行参数已变更 |
| 项目结构 | AGENTS.md, ARCHITECTURE.md | 文件不存在导致困惑 |
| 实验架构 | 01-分层实验架构设计.md | 架构已废弃 |

---

## 附录：文档状态速查表

| 文档 | 状态 | 优先级 | 行动 |
|------|------|--------|------|
| README.md | 🟡 需更新 | 高 | 修正使用方法 |
| AGENTS.md | ❌ 严重过时 | 紧急 | 重写或删除 |
| ARCHITECTURE.md | ❌ 多处错误 | 高 | 修正公式和结构 |
| docs/01-分层实验架构设计.md | ❌ 架构废弃 | 中 | 标记为历史 |
| docs/09-论文大纲.md | ❓ 存疑 | 中 | 需用户确认 |
| docs/11-文档一致性检查.md | ❓ 需验证 | 低 | 验证参数一致性 |
| docs/MIM_CORRECTION_SUMMARY.md | 🟡 路径过时 | 低 | 更新路径引用 |

---

**建议下一步**: 先重写 AGENTS.md 和修正 README.md，然后讨论论文大纲的适用性。
